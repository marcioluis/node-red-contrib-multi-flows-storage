{
	"name": "node-red-contrib-galgo",
	"version": "0.0.1",
	"license": "MIT",
	"scripts": {
		"local": "rm -rdf dist && npx tsc && cp package.module.json dist/package.json && ln -s node_modules dist/node_modules",
		"prod": "rm -rdf dist && npx tsc -p tsconfig.prod.json && cp package.module.json dist/package.json"
	},
	"node-red": {
		"nodes": {
			"flows": "main.js"
		}
	},
	"dependencies": {
		"fs-extra": "^9.1.0",
		"js-yaml": "^4.0.0",
		"node-red": "^1.2.9"
	},
	"devDependencies": {
		"@types/fs-extra": "^9.0.9",
		"@types/js-yaml": "^4.0.0",
		"@types/node": "^14.14.37",
		"@types/node-red": "^1.1.1",
		"typescript": "^4.2.3"
	}
}
